{
  "name": "cryptopulse-tests",
  "private": true,
  "description": "Test suite only — index.html itself has no build step and is deployed as-is to GitHub Pages, unaffected by anything here.",
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
